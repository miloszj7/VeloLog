# Credentials for a real Django user the E2E suite logs in as (auth.setup.ts).
# Any account works — no admin/staff rights needed.
# Copy this file to .env in this same directory and fill in the real values —
# .env is gitignored, this file is not.
E2E_USERNAME=
E2E_PASSWORD=