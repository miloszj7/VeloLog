{
  "permissions": {
    "allow": [
      "Bash(npm *)",
      "Bash(npx *)",
      "Bash(node *)",
      "Read",
      "Edit",
      "Write"
    ],
    "ask": [
      "Bash(curl *)",
      "Bash(wget *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); case \"$f\" in *.py) out=$(uv run black --quiet -- \"$f\" 2>&1); code=$?; if [ $code -ne 0 ]; then printf '%s\\n' \"$out\" >&2; exit 2; fi ;; esac",
            "timeout": 30,
            "statusMessage": "black formatting..."
          }
        ]
      }
    ]
  }
}